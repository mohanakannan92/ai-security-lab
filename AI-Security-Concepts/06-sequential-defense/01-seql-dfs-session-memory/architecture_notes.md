Simple Explanation
Instead of checking only the current message,
the system checks the current message plus earlier behavior.
Example
1. Hi                     → allow
2. How do you work?       → allow
3. What rules are you following? → block

Reason:

benign → probing → sensitive